[package]
name = "distilllab-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
schema = { package = "distilllab-schema", path = "../schema" }
memory = { package = "distilllab-memory", path = "../memory" }
uuid = { version = "1.18.1", features = ["v4"] }
